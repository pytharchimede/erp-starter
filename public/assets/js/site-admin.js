document.addEventListener('DOMContentLoaded',()=>{const preview=document.querySelector('.site-admin-preview');if(!preview)return;document.querySelectorAll('[data-color-picker] input').forEach(input=>{const update=()=>{const map={primary_color:'--preview-primary',secondary_color:'--preview-secondary',accent_color:'--preview-accent'};if(map[input.name])preview.style.setProperty(map[input.name],input.value);};input.addEventListener('input',update);input.addEventListener('change',update);});});
